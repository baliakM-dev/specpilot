# Prompt Templates

Documents the versioned prompt templates used by the AI Analysis module (`PromptTemplateService`), including the exact template text for each `promptVersion` referenced by stored `AiGeneration` records.

**Status:** not yet implemented. To be completed in Phase 12 (AI backend) of [`TASKS.md`](../../TASKS.md), once the actual prompt templates exist in `backend/src/main/resources/prompts/`.