# API Design

REST API conventions and the full endpoint reference (paths, request/response DTOs, status codes, pagination), matching the actual controllers and OpenAPI spec.

**Status:** not yet implemented. To be completed in Phase 26 (Documentation) of [`TASKS.md`](../../TASKS.md); until then, the live source of truth is the running Swagger UI at `/swagger-ui.html`.