# Database Model

Entity-relationship diagram and description of every table (users, projects, project_members, requirements, requirement_versions, ai_generations, reviews, review_comments, adr_documents, audit_logs), matching the actual Flyway migrations.

**Status:** not yet implemented. To be completed in Phase 26 (Documentation) of [`TASKS.md`](../../TASKS.md), kept in sync with the migrations under `backend/src/main/resources/db/migration` as each module is built.