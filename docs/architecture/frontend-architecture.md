# Frontend Architecture

Frontend structure (`app`, `router`, `api`, `auth`, `pages`, `features`, `shared`), state management approach (React Query), form/validation conventions, and routing/auth-guarding strategy.

**Status:** not yet implemented. To be completed in Phase 26 (Documentation) of [`TASKS.md`](../../TASKS.md), once the frontend foundation and feature modules actually exist.