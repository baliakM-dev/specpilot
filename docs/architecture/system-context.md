# System Context

High-level context diagram: SpecPilot users, the SpecPilot system, and external systems it depends on (Keycloak, AI provider).

**Status:** not yet implemented. To be completed in Phase 26 (Documentation) of [`TASKS.md`](../../TASKS.md), once the system is fully built and can be described as it actually exists rather than as planned.