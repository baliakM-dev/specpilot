# BFF Login Flow

Step-by-step sequence diagram of the Backend-for-Frontend OIDC login flow: browser → frontend → backend → Keycloak → backend session → `/api/auth/me`.

**Status:** not yet implemented. To be completed in Phase 5 (Backend Security / BFF) of [`TASKS.md`](../../TASKS.md), once the login flow is actually implemented and testable end-to-end. See [ADR 0002](../adr/0002-use-bff.md) for the underlying decision.